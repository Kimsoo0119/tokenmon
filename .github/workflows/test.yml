name: test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: ['20', '22']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm
      # 테스트는 순수 Node 모듈만 다루므로 Electron 바이너리(~100MB)는 받지 않습니다
      - run: npm ci --ignore-scripts
      - run: npm test
